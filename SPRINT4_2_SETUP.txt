SAINT SANCTUARY — SPRINT 4.2
HOLDER VERIFICATION ENGINE

1. Run in Supabase SQL Editor:
   supabase/sanctuary_sprint4_2.sql

2. Existing Vercel variables:
   SUPABASE_URL
   SUPABASE_SERVICE_ROLE_KEY

3. Optional but recommended Vercel variable:
   SOLANA_RPC_URL

If SOLANA_RPC_URL is not configured, the system tries public Solana RPC
providers automatically.

Rule:
- Minimum required: 1,000,000 SAINT
- Cache duration: 5 minutes
- SAINT mint: GUdYAzh14TQcwUSBw79rnFJHZCv64fugTEsq1etDpump

Implemented:
- Only an authenticated wallet session can request a balance check.
- Reads the SAINT token balance from Solana.
- Stores the result in sanctuary_holders.
- Uses a 5-minute Supabase cache.
- Shows progress, balance, required amount and remaining amount.
- Shows “Buy more $SAINT to unlock access to the Sanctuary.”
- Displays “💙 Welcome to the Sanctuary.” when eligible.
- Enter The Sanctuary becomes visually unlocked when eligible.

Not included yet:
- Protected Telegram invite delivery.
- Final Enter The Sanctuary redirect.
